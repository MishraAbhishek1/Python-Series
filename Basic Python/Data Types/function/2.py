def my_function(parameters):
    # body of the function
    return Value

# Example
def add_numbers(a, b):
    return a + b
result = add_numbers(5, 10)
print("Sum is:", result)

# logc buildup
def my_function(m):
    m = ['x', 'y','z']
    for item in m:
        if item[0] == 'x':
            return True
        else:
            return False
print(my_function([0]))