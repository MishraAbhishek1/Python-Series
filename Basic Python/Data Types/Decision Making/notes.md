when we ue a condition simply when we know like if tommaro i havy rain than print the message and rain is not come else odition run its is very usable in real world projects concepts >

in the python if when we use heck the condition .
when conditon is true  than code is run and else part run

for example see the syntax

if condition:
 # true block

else:
  # false block


>> why we need condition because whe we apply a conditon we do a decision making here  abd decison making apply all software build projects